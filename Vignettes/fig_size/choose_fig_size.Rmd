---
title: 'Choose size '
author: "Andriy Koval"
output:
  pdf_document:
    fig_width: 8
    toc: yes
  html_document:
    css: ~/GitHub/Longitudinal_Models_of_Religiosity_NLSY97/www/css/thesis.css
    fig.retina: 2
    fig_width: 8
  word_document:
    fig_width: 6.5
---

<!--  Set the working directory to the repository's base directory; this assumes the report is nested inside of only one directory.-->
```{r, echo=F, message=F} 
require(knitr)
opts_knit$set(root.dir='../../')  #Don't combine this call with any other chunk -especially one that uses file paths.

```

```{r set_options, echo=F, message=F}
require(knitr)
# getwd()
opts_chunk$set(
  results='show', 
  message = TRUE,
  comment = NA, 
  tidy = FALSE,
#   fig.height = 4.8, 
#   fig.width = 6.5, 
  out.width = NULL,
  fig.path = 'figure_rmd/',     
  dev = "png",
  dpi = 400
)
echoChunks <- FALSE
warningChunks<- FALSE
options(width=120) #So the output is 50% wider than the default.
```
Helps you choose the options for

+ fig.width
+ fig.height


```{r DeclareGlobals, echo=echoChunks, message=FALSE}
attcol8<-c("Never"="#4575b4",
           "Once or Twice"="#74add1",
           "Less than once/month"="#abd9e9",
           "About once/month"="#e0f3f8",
           "About twice/month"="#fee090",
           "About once/week"="#fdae61",
           "Several times/week"="#f46d43",
           "Everyday"="#d73027")

baseSize<- 14
colNA<- "#696969"
```

```{r LoadPackages, echo=echoChunks, message=F}
require(dplyr)
require(grid) #For the (zero) length of the tick marks
require(ggplot2)
```

```{r LoadData, echo=echoChunks, message=T}
dsL<-readRDS("./Data/Derived/dsL.rds")
```

```{r DefineCommonGraph, echo=echoChunks, message=F}
# The graph in this report is reproduced under various figure option conditions.
ds<- dsL %>% dplyr::filter(id %in% c(4,25,45,47),year %in% c(2000:2011)) %>% dplyr::select(idF,year,attend) %>%
  mutate(time=year-2000)
p<-ggplot(ds, aes(x=time,y=attend, color=idF))
# geoms
p<-p+ geom_line(size=.4)
p<-p+ geom_point(size=2)
# scales
p<-p+ scale_y_continuous("Church attendance",
                     limits=c(0, 8),
                     breaks=c(1:8))
p<-p+ scale_x_continuous("Years since 2000",
                   limits=c(0,11),
                   breaks=c(0:11))
p <- p + scale_color_manual(values=c("black","red","green", "purple"))
# annotations
p<-p+ labs(title=paste0("In the past year, how often have you attended a worship service?"))
p <- p + guides(color = guide_legend(title="Respondents"))
# themes
p <- p + ggplot2::theme_bw()
p <- p + ggplot2::theme_bw(base_size=baseSize)
p <- p + ggplot2::theme(title=ggplot2::element_text(colour="gray20",size = 12)) 
p <- p + ggplot2::theme(axis.text=ggplot2::element_text(colour="gray40"))
p <- p + ggplot2::theme(axis.title=ggplot2::element_text(colour="gray40"))
p <- p + ggplot2::theme(panel.border = ggplot2::element_rect(colour="gray80"))
p <- p + ggplot2::theme(axis.ticks.length = grid::unit(0, "cm"))
p



height1<- 3.0
height2<- 3.4
height3<- 3.8
height4<- 4.2
height5<- 4.6
height6<- 5

# width1<- 6.5
# width2<- 6.5
# width3<- 6.5
# width4<- 8
# width5<- 8
# width6<- 8  

owidth1<- NULL
owidth2<- NULL
owidth3<- NULL
owidth4<- NULL
owidth5<- NULL
owidth6<- NULL




```


# Comparing produced graphs

##  Slot 1
```{r slot_1, echo=echoChunks, message=T,fig.height = height1,  out.width=owidth1  }
p
```


##  Slot 2
```{r slot_2, echo=echoChunks, message=T, fig.height = height2, out.width=owidth2}
p
```

##  Slot 3
```{r slot_3, echo=echoChunks, message=T, fig.height = height3, out.width=owidth3}
p
```


##  Slot 4
```{r slot_4, echo=echoChunks, message=T, fig.height = height4,  out.width=owidth4}
p
```


##  Slot 5
```{r slot_5, echo=echoChunks, message=T, fig.height = height5, out.width=owidth5}
p
```


##  Slot 6
```{r slot_6, echo=echoChunks, message=T, fig.height = height6,  out.width=owidth6}
p
```